# ذاكرة المشروع — منصة «جسر إلى الصين» التعليمية

> **هذا الملف هو الذاكرة الدائمة للمشروع.**
> إن بدأت جلسة جديدة، أو فقدتُ السياق، أو بدأت أسأل «أين المستودع؟» و«أين Vercel؟» — **اقرأ هذا الملف أولاً قبل أي شيء آخر.**
> كل ما فيه مُثبَت بالفحص المباشر، لا بالتخمين. آخر تحديث: 2026-08-24.

---

## ٠. المصادر — لا تسأل عنها مرة أخرى

| المصدر | القيمة المؤكدة |
|---|---|
| **مستودع GitHub** | `https://github.com/Jh4k5/Moehsk1` — **مربوط بهذه المحادثة منذ بدايتها**، وهو الذي يعطي الوصول إلى ملفات المشروع |
| **المجلد المحلي** | `/home/user/Moehsk1` |
| **فرع العمل** | `claude/platform-business-analysis-p53oiq` |
| **الفرع الرئيسي** | `main` |
| **الموقع المنشور** | `https://moehsk1.vercel.app/` — **محجوب عن شبكة هذه الجلسة** (403 من وكيل الشبكة)، فلا يمكن فحصه مباشرة |
| **التقنيات** | Next.js 16 (App Router) · React 19 · TypeScript · Tailwind 4 · Zustand · Supabase |

### ⚠️ ثلاثة التباسات تكررت — احسمها هكذا ولا تعد إليها

1. **`jisr-to-china` ليس هذا المشروع.** يوجد مشروع Vercel ومشروع Supabase بهذا الاسم، وكلاهما يخصّ **موقع شركة الاستشارات الدراسية** (جامعات، قبولات، عملاء محتملون، مقالات) — لا علاقة له بمنصة التعلّم. المالك أكّد ذلك صراحةً.
2. **`Jh4k5/mohammed-hsk1` ليس هذا المستودع.** أداة HTML ساكنة بملف واحد، لا صلة لها.
3. **حساب Vercel المتصل بهذه الجلسة لا يرى مشروع `moehsk1`.** هذه حقيقة مقاسة، لا رأي. لا تدّعِ أن الموقع غير موجود — بل قل: «غير مرئي من هذه الجلسة».

### ✅ حُسم نهائياً: حسابان مختلفان (لقطات شاشة من المالك، 2026-08-25)

**السبب الجذري لكل الالتباس: للمالك حسابان، وأنا متصل بالخطأ منهما.**

| | حساب المالك الحقيقي | الحساب المتصل بجلستي |
|---|---|---|
| البريد | **`mhomnyf8@gmail.com`** | `mnmwrllm8@gmail.com` |
| Vercel | `moehsk1` · `life-os` · `jisr-for-chinese` | `jisr-to-china` فقط |
| Supabase | مؤسستا «Jisr to Chinese» و«Life os» | مؤسسة «Jisr» وحدها |

**⇒ لا تقل «المشروع غير موجود» أبداً.** قل: «غير مرئي من هذه الجلسة». كل ما ظننّاه مفقوداً كان موجوداً في الحساب الآخر.

**وما أثبتته اللقطة أيضاً:**
- مشروع Vercel `moehsk1` **مربوط فعلاً** بـ`Jh4k5/Moehsk1`، **ويبني معاينات من فرع العمل** (`claude/platform-business-analysis-…`). أي أن عملي يصل إليه بالفعل عبر Preview.
- لكنه يقول **«No Production Deployment»** — لا نشر إنتاجي إطلاقاً. وهذا يفسّر سلوك `moehsk1.vercel.app`.
- قاعدة البيانات التي بنيتُها (`moehsk1-platform`, `dxexmiyyjpvvmmcawkrl`) **حقيقية وتعمل، لكنها في الحساب الخطأ** — تحتاج إما إعادة ربط الموصّل، أو إنشاء نظيرتها في المؤسسة الصحيحة.

---

### 🔎 سبب الالتباس — مُثبَت بالفحص (وكيل Vercel، 2026-08-24)

**العلامة التجارية واحدة والمنتجان مختلفان.** شركة «جسر إلى الصين» للاستشارات لها **موقعان**:

| | موقع الاستشارات | منصة التعلّم (هذا المشروع) |
|---|---|---|
| النطاق | `jisr-to-china.vercel.app` | `moehsk1.vercel.app` |
| المستودع | `Jh4k5/-` فرع `claude/jisr-china-trilingual-platform-wzojbr` | **`Jh4k5/Moehsk1`** |
| قاعدة البيانات | Supabase `hwqvvhnbfnkeqfzpofaa` | لا توجد بعد |
| المحتوى | جامعات، قبولات، فيزا، قصص نجاح | دروس، مفردات، HSK |

**الدليل القاطع** من صفحة موقع الاستشارات نفسها، تحت بطاقة «برامج اللغة الصينية (HSK)»:
> **«لسنا الجهة التي تُدرّس اللغة الصينية»**

وصفر ورود لكلمة «كلمة» في الصفحة كلها — لا «١٥٠ كلمة» ولا «٤٠٥ كلمة». فمعلومة «المستوى الأول ١٥٠ كلمة» **ليست من هذا المستودع**.

**⇒ الخلاصة: للمالك حساب Vercel ثانٍ** يستضيف `moehsk1.vercel.app`. الدليل: مستودع `Jh4k5/Moehsk1` حقلُ homepage فيه = `https://moehsk1.vercel.app`، ومستودع `Jh4k5/Life-os` حقله = `life-os-pearl-nine.vercel.app`، وكلاهما **غير موجود** في الحساب المتصل هنا (`mnmwrllm8@gmail.com` / فريق `mnmwrllm8-3334s-projects`، خطة Hobby، مشروع واحد فقط).
وتأكيد إضافي أن هذا المستودع هو الصحيح: `src/app/[locale]/layout.tsx:37` يحمل `const SITE = 'https://moehsk1.vercel.app'` مكتوباً صراحةً.

**عائق النشر من هنا:** لا يوجد **GitHub Login Connection** على حساب Vercel (`originConnections: []` و`linkedProjects: []`). الحل: ربط حساب GitHub «Jh4k5» بحساب Vercel من الإعدادات، ثم استيراد `Jh4k5/Moehsk1` بفرع إنتاج `main`.

---

### 🛟 نقطة الرجوع — النسخة القديمة محفوظة

الفرع **`v1-legacy`** على GitHub (عند `e74fb38`، ٢٥ يوليو ٢٠٢٦) يحمل **المنصة القديمة كاملةً**: الموسوعة ذات الـ١٩ قسماً، قبل بدء التحويل مباشرةً (أول التزام من التحويل هو `0908c28`).

**مُتحقَّق منه:** لا `src/lib/payments`، ولا `src/lib/supabase`، ولا `supabase/migrations`، ولا `src/data/units`، ولا توجيه `[locale]` — أي أنها فعلاً النسخة الأحادية القديمة (`src/app/page.tsx` الوحش). الفرق بين النسختين: **٢٩٥ ملفاً، ‎+٣٤٬٣٧٢ / −١٤٬٣٥٢**.

```bash
git fetch origin && git checkout v1-legacy    # للعودة إليها
```

⚠️ **دفع الوسوم (tags) محجوب** بوكيل الشبكة (403)، فاستُعمل فرع بدلاً من وسم. دفع الفروع يعمل عادياً.
⚠️ **إنشاء المستودعات محجوب** أيضاً (`403 Resource not accessible by integration`) — لا يمكنني إنشاء مستودع ثانٍ؛ يفعله المالك بنفسه.

---

## ١. فكرة المنصة — الجوهر الذي يجب ألّا يُنسى

منصة عربية لتعلّم الصينية على **المنهج الجديد**، ثلاثة مستويات: HSK 1 و 2 و 3.

**المشكلة التي تحلّها:** المنصة القديمة كانت **موسوعة** — ١٩ قسماً مسطحاً (مفردات، قواعد، جمل، قصص، محادثات، تمارين، ألعاب، محاكي امتحان، أسئلة يومية، قاموس بصري، بينين، حروف، نطق، خريطة طريق، إنجازات، معلّم ذكي، دروس). الطالب يدخل **فيتوه**: لا يعرف من أين يبدأ ولا ماذا يذاكر.

**الحل المتفق عليه:** تحويلها إلى **مسار تعليمي متدرّج** بأسلوب دولينجو:
- كل قسم من الـ١٧ يصير **نوع نشاط داخل الدرس**، لا قسماً منفصلاً.
- الدرس الواحد يمارس فيه الطالب **كل المهارات معاً**: يقرأ، يكتب الحرف بترتيب ضرباته، ينطق فيُقيَّم، يحاور، يقرأ قصة، يلعب.
- الأقسام الحرّة **تبقى متاحة للمراجعة، اختيارية، خارج المسار الإلزامي**.
- الكلمة الجديدة تمرّ **بست مراحل**: أرِني الحرف ← اسمعه ← فكّكه إلى جذوره ← تعبير الحفظ ← اكتبه بترتيب ضرباته ← استعمله في جملة.

**البنية:** المستوى ← الدرس (١٥/١٥/١٨) ← **الوحدة** (٤–٦ لكل درس، ٥–٨ دقائق) ← تيار أنشطة (١٢–٢٠).
النتيجة المولّدة فعلاً: **١٩١ وحدة** (٦٩ + ٣٤ + ٨٨).

**المحتوى الفعلي:** ١٬٠٧٩ كلمة · ٤٨ درساً · ١٠٣ قواعد. (HSK1 = **٤٠٥ كلمة**، لا ١٥٠.)

---

## ٢. متطلبات المالك — بنصّه، ومنها ما خالفتُه

### 🔴 المتطلب الذي خالفتُه — وهو أخطر ما في هذا الملف

> **«يستطيع أي أحد أن يختار مستوى عندما يقوم بتسجيل الدخول ليبدأ به مباشرة»**
> **«كل منهما منصة أو كل منه مستوى الوحدة»** — أي كل مستوى منصة قائمة بذاتها.

**ما بنيتُه بدل ذلك:** مسار واحد يجبر الجميع على البدء من **HSK1 / درس ١ / وحدة ١** مع فتح تسلسلي، والمجاني دائماً من HSK1.
**لماذا هذا كارثي:** طالب مستواه HSK2 لا يمكنه البدء من مستواه، والدروس المجانية تُعطى له في مستوى لا يريده.
**كلام المالك:** «ما في أحد يختار مستواه… إنه شخص في المستوى الثاني أكيد ما بيبدأ من الأول، فتعطيه دروس مجانية في المستوى الثاني».
**⇒ هذا أول ما يُصلَح.**

### بقية المتطلبات المثبتة

| # | المتطلب | الحالة |
|---|---|---|
| ١ | **الهاتف أولاً — ٩٠٪ من المستخدمين على الهاتف**، توافق ١٠٠٪ | ⚠️ يحتاج تحقّقاً |
| ٢ | تسجيل دخول **جوجل + بريد** حقيقي | 🔴 المالك: «لا يوجد تسجيل دخول حقيقي أصلاً» |
| ٣ | نظام اشتراك: **درس أو درسان مجاناً** ثم يُقفَل | ⚠️ القاعدة موجودة لكن مربوطة بـHSK1 فقط |
| ٤ | **أكواد تفعيل** للمؤثرين ومن يُعطَون مجاناً (مختلفة عن الدفع العادي) | 🔴 المالك: «ما في أكواد أصلاً» |
| ٥ | **السعر لا يُكتب في الكود** — يُحرَّر من لوحة التحكم (`app_config`) | ✅ مطبَّق |
| ٦ | لوحة تحكم للمالك: مستخدمون، اشتراكات، أكواد، مفاتيح ميزات، إحصاءات | ✅ الكود موجود، ❌ لا يعمل بلا قاعدة بيانات |
| ٧ | **الفهرسة في جوجل** — تظهر عند البحث عن تعلّم الصينية | ✅ sitemap + بيانات منظّمة |
| ٨ | **لغتان** عربي/إنجليزي، كلاهما يعمل بالكامل | 🔴 الإنجليزية سطحية — الواجهة مترجمة والمحتوى لا |
| ٩ | الهوية البصرية «جسر إلى الصين» حاضرة بقوة في كل شاشة | ✅ مطبَّقة |
| ١٠ | معلّم ذكي يعمل (كان معطّلاً) | ✅ بُني بمستويين |
| ١١ | منصة خفيفة، قابلة للتحول لتطبيق جوال لاحقاً | ✅ في البنية |

---

## ٣. الهوية البصرية — أرقام لا أذواق

| اللون | Hex | الدور |
|---|---|---|
| الأزرق الموثوق (كحلي) | `#1B3A6B` | **لون الإجراء** والأسطح الأساسية |
| الأحمر الصيني | `#C8102E` | الخطأ والتمييز |
| الذهبي الإمبراطوري | `#D4AF37` | الإنجاز |
| العاجي | `#F7F4EF` | الخلفية الفاتحة |
| الفحمي | `#2A2A2A` | نص المتن |

**ثلاث قواعد مشتقة من قياس التباين (لا تُخالَف):**
- الأحمر على الكحلي = **1.91** (رسوب) ⇒ الوضع الداكن يستعمل `#E23A4E` (4.27).
- الذهبي نصاً على العاجي = **1.92** (رسوب) ⇒ الذهبي **شكل** على الفاتح، **نص** على الداكن فقط (8.62).
- **الكحلي زر الإجراء، لا الأحمر** — لأن الأحمر يعني «إجابة خاطئة» داخل التمارين. الحكم لا يعتمد على اللون وحده أبداً: أيقونة وشكل وموضع معه.

**الخطوط:** Cairo للعناوين · Tajawal للنصوص · Noto Sans SC للصينية.
**الأرضية الداكنة:** `#0C1626` و `#122033` — مشتقة من الكحلي، لا رمادي محايد.
المكوّنات تقرأ **رموزاً دلالية** (`--action`, `--text-*`, `--surface-*`, `--verdict-*`) لا ألواناً صريحة.

---

## ٤. الحالة الحقيقية — ما يعمل وما لا يعمل

### ✅ مبنيّ ومُتحقَّق منه في الكود
- بوابة دفع محايدة (Paddle / Lemon Squeezy / بلا) مع تحقّق توقيع HMAC ومنع تكرار الأحداث
- تسجيل دخول جوجل + رابط سحري، تحديث الجلسة في `middleware.ts` (**كود** فقط)
- لوحة تحكم كاملة + زر تفعيل أول مدير (بُني حديثاً بدل سطر console)
- حاجز المحتوى المدفوع: `content-source.ts` بـ`server-only` + `/api/content/[level]`
- ١٩١ وحدة مولّدة + محرك أنشطة حتمي (١٧ نوع نشاط)
- ١٠ ملفات migration في `supabase/migrations/` جاهزة للتطبيق
- فحوص آلية: `check:activities` `check:progress` `check:payments` `check:admin` `check:paywall`

**تحقّق مستقل (وكيل سلامة المستودع، 2026-08-24):** كل الأنظمة الفرعية **موجودة وموصولة** (مدفوعات، دخول، لوحة، معلّم، منهج، صلاحيات)، و**العشر migrations كاملة ومتتابعة**، و`tsc` و`lint` و`build` و`check` كلها **خضراء**.

### 🧹 جولة تنظيف — نُفِّذت ٢٠٢٦-٠٨-٢٤
- **حُذف ١٬٩٠٣ أسطر كود ميت** (٩ ملفات)، بعد التحقق من كلٍّ منها **بالمسار الدقيق** لا بالاسم: `features/admin/data/{supabase-source,types}.ts` (فرغ المجلد فحُذف)، `components/{LessonSystem,PomodoroTimer,LangController}.tsx`، `hooks/use-mobile.ts`، `features/shared/use-level-stats.ts`، `data/hsk{2,3}/roadmap{2,3}.ts`.
  المراجع الوحيدة المتبقية كانت **تعليقات تذكر الأسماء**، لا استيرادات. وصُحّح تعليق في `(app)/layout.tsx` كان يشير إلى `use-mobile.ts` المحذوف.
- **أُعيد توليد `units.generated.ts`** — الانحراف كان محصوراً في HSK3 تماماً كما تنبّأ التدقيق: **HSK1 صفر، HSK2 صفر، HSK3 ١٧٤ وحدة**. الأنشطة ٧٬٣٦٩ ← ٧٬٣٨٩.
- **صفر ثغرات npm** بعد `npm audit fix` — ولم تتغيّر أي تبعية مباشرة، اللوك فقط.
- **`npm run verify` جديد** = `check` ثم `build` ثم `check:paywall` بالترتيب الصحيح (فاحص التسريب يمسح الحزم المبنية، فلا يصحّ قبل البناء). كان خارج السلسلة فلا يعمل إلا يدوياً.

⚠️ **لا تحذف أقسام المكتبة الـ١٥ ظنّاً أنها يتيمة** — تُحمَّل ديناميكياً عبر `library/registry.tsx`، فلا يراها grep الساكن. وكذلك `conversations2/3.ts` يستهلكها `build-units.js` وقت البناء.
⚠️ **`hanzi-writer` يُحمَّل من jsDelivr** (`HanziBoard.tsx`, `HanziSection.tsx`) — محجوب في بيئة التطوير هذه، فيظهر `ERR_TUNNEL_CONNECTION_FAILED` على `/library/hanzi`. ليس عيباً؛ يعمل في الإنتاج.

### 🧾 دَين تقني متبقٍّ
- **`main` متأخر عن فرع العمل** — إصلاحات الأعطال الكارثية لم تصل إليه بعد، فالموقع الحيّ (إن كان يخدم `main`) ما زال يحمل كوداً معطوباً.

### ✅ حُسم: قاعدة البيانات مطبَّقة فعلاً (تدقيق مباشر عبر Supabase MCP، ٢٠٢٦-٠٨-٢٦)

كان مكتوباً هنا سابقاً «لا توجد قاعدة بيانات مطبَّقة عليها schema المنصة في أي مكان». **هذا لم يعد صحيحاً وقد كان تخميناً من أسماء المشاريع.** القياس المباشر:

- المشروع العامل: `dxexmiyyjpvvmmcawkrl` — `https://dxexmiyyjpvvmmcawkrl.supabase.co`.
- الـmigrations مطبَّقة، و`public.profiles` فيه **٣ مستخدمين حقيقيين**، و`auth.identities` فيه هويات جوجل حقيقية بـ`sub` و`iss` ورموز وصول.
- الدالة `on_auth_user_created` **مثبّتة وتعمل ٣ من ٣**: تستخرج `display_name` من `raw_user_meta_data->>'full_name'` بنجاح، **بالعربية أيضاً**. `search_path` مثبّت على `public, pg_temp`، وكل كائن مؤهَّل بالschema، و`on conflict (id) do nothing` يمتصّ التكرار. **لا تعد إلى الشك فيها.**

⇒ العائق ليس غياب القاعدة. العائق التالي وحده هو ما يمنع الدخول.

### 🔴 سبب العطل الحقيقي: **مسافة واحدة في إعداد Site URL بلوحة Supabase**

**هذا هو العطل الذي استهلك ساعات من التخمين. مُثبَت بالقياس في سجلّات GoTrue وedge logs، لا استنتاجاً.**

الإعداد `Site URL` مكتوب هكذا: `https: //moehsk1.vercel.app` — بمسافة بين `https:` و`//`.

**ما يحدث بالضبط:**
1. تسجيل دخول جوجل **ينجح تماماً**. خمس رحلات كاملة عبر `accounts.google.com` في السجلّات، و`/auth/v1/authorize` يردّ `302 Redirecting to external provider`.
2. العطب في **رحلة العودة**: `/auth/v1/callback` يردّ `302` نحو ذلك العنوان المشوَّه.
3. `https: //moehsk1.vercel.app` بلا authority قابل للتحليل، فيحلّه المتصفح **نسبةً إلى أصل Supabase نفسه**، فينتهي عند:
   `GET /auth/v1/%20/moehsk1.vercel.app?code=…` ⇐ **401**
4. وهذا `401` هو حرفياً الرسالة التي يراها المالك:
   `{"hint":"No apikey request header or url param was found.","message":"No API key found in request"}`

**المصدر داخل GoTrue:** الحقل `referer` في سجلّ الطلب يأتي من `utilities.GetReferrer()`، وهي ترجع بالترتيب: `redirect_to` صالح ← ترويسة `Referer` صالحة ← **`config.SiteURL` كملاذ أخير**. رجوعها إلى Site URL يعني أن الاثنين الأولين **رسبا في قائمة السماح**. ونفس السجلّ يسجّل `http://localhost:3000` **سليماً بلا تشويه** — فالمسافة في الإعداد لا في العرض.

**العلاج (بيد المالك، في اللوحة لا في الكود):** Authentication → URL Configuration
1. `Site URL` = `https://moehsk1.vercel.app` — احذف المسافة.
2. `Redirect URLs`: أضف `https://moehsk1.vercel.app/**`، أبقِ `http://localhost:3000/**`، وأضف نمط معاينات Vercel.
**دليل النجاح:** ظهور صفّ في `auth.sessions` واختفاء الصفّ المقابل من `auth.flow_state`.

**فرضيات ماتت بالقياس — لا تُحيها:**
| الفرضية | لماذا ماتت |
|---|---|
| «`/auth/v1/authorize` يشترط `apikey`» | ردّ `302` بنجاح **بلا** `apikey` إطلاقاً في محاولتَي 12:41 و12:43 |
| «الحزمة المنشورة قديمة فالإصلاح لم يصل» | من 13:09 فصاعداً **كل** رابط authorize يحمل `&apikey=…` — والعطل نفسه استمرّ في 13:25 و13:33 |
| «الدالة `on_auth_user_created` تُسقط التسجيل» | نجحت ٣ من ٣ وبياناتها في القاعدة |
| «جوجل غير مفعّل أو مفاتيحه خاطئة» | ٥ رحلات كاملة وهويات حقيقية مخزَّنة |

⇒ إضافة `apikey` في `queryParams` كانت علاجاً لتشخيص خاطئ، ولا نفع لها. تُزال.

**عطلان منفصلان في الرابط السحري** (غير مرتبطين بما سبق، وسيظهران بعد إصلاح Site URL):
- المالك كتب `mnmwrlllm8@gmail.com` بثلاث لامات ⇒ `email_address_invalid`.
- ثم المحاولات المتكررة اصطدمت بـ`429 over_email_send_rate_limit` — **سقف الخطة المجانية رسالتان في الساعة**.
⇒ يجب أن تعرض الواجهة هذين بنصّين مختلفين واضحين، لا برسالة فشل عامة.

**حاجز تالٍ معروف ومنتظَر:** الثلاثة كلهم `role = 'user'`. حتى بعد نجاح الدخول ستبقى لوحة التحكم مقفلة حتى تُرقّى صلاحية المالك — ويلزم لذلك تحديث بـ`service_role` على `public.profiles.role`، لأن `tg_profiles_guard_role` يمنع أي مستخدم من ترقية نفسه، ولا مدير بعدُ ليقوم بها من الواجهة.

### 🐛 أخطاء مكتشفة ومؤكَّدة
| الخطأ | الدليل | الحالة |
|---|---|---|
| ترتيب الكلمات داخل الوحدة تعليمياً خاطئ | `units.generated.ts:18` — وحدة ١:١:١ تبدأ بالكلمة ٤٦ = **吗** (أداة استفهام) قبل الكلمة ١ = **你好** | 🔴 مفتوح |
| الإنجليزية تترجم الواجهة لا المحتوى | `ActivityView.tsx` يقرأ `word.meaning` دائماً ولا يقرأ `word.english` رغم وجودها في البيانات | 🔴 مفتوح |
| لا يتوافق مع اللابتوب | `globals.css` — `.j-path`/`.j-home` محدودان بـ`max-width:30rem` فيبقى ~٦٠٠px فراغاً | 🔴 مفتوح |
| **لا اختيار للمستوى** | لا واجهة اختيار مستوى إطلاقاً — المسار يبدأ دائماً من HSK1 | 🔴 مفتوح — **الأهم** |
| **`units.generated.ts` قديم — وحدات HSK3 مبنية على لقطة مفردات سابقة** | ٨٠٦ من ٢٬٣١٢ سطراً تختلف عند إعادة التوليد (٣٥٪)، والانحراف **محصور في HSK3** (`wordIds` ضمن ٣٠٠١–٣٤٧٩). آخر توليد عند `f1279fd`، ثم نزل **١٠ التزامات بيانات** بعده (٨ دفعات تعابير حفظ HSK3 + ٣٣ محادثة) ولم يُعَد التوليد. المولّد **حتمي** (٣ تشغيلات متطابقة) ⇒ قِدَم حقيقي لا عشوائية | 🔴 مفتوح — العلاج: `node scripts/build-units.js` ثم التزام |
| عطل تحطّم الجلسات (قصص + أسئلة يومية) | `SessionRunner.tsx` كان يمرّر قصصاً خاماً | ✅ **أُصلح** (commit `942e76e`) |
| باب خلفي: مفاتيح تراخيص مكتوبة في الكود | `api/validate-license/route.ts` | ✅ **حُذف** |
| تسريب ٩١٨KB محتوى مدفوع في حزم المتصفح | `levels.ts` كان يستورد كل المستويات ساكناً | ✅ **أُصلح** |
| حلقة الهدف اليومي عالقة على ٠٪ | عدّاد لا يُزاد أبداً في `store.ts` | ✅ **أُصلح** |

---

## ٥. كيف تُفعَّل لوحة التحكم (لا يوجد يوزر/باسورد — بالتصميم)

**لا توجد كلمات مرور في هذه المنصة إطلاقاً**، عمداً: جوجل أو رابط سحري بالبريد فقط. لا يوجد «حساب جاهز» — المالك ينشئه بنفسه.

0. **أولاً وقبل كل شيء: أصلح `Site URL` في لوحة Supabase** (احذف المسافة — انظر §٤). بدون هذا لن ينجح أي تسجيل دخول مهما فعلتَ بالكود أو بالمتغيّرات.
1. اضبط على الاستضافة: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, و`ADMIN_BOOTSTRAP_EMAILS` = بريد المالك. **أعد النشر** بعدها.
2. سجّل الدخول من `/ar/sign-in` **بنفس البريد**.
3. ادخل `/ar/admin` — يظهر **زر «فعّل صلاحية المدير»** (بُني حديثاً؛ سابقاً كان يتطلب لصق `fetch` في console).
4. بعد النجاح: **احذف `ADMIN_BOOTSTRAP_EMAILS`** — الصلاحية صارت في `profiles.role` بقاعدة البيانات.
5. الأكواد تُنشأ من اللوحة ← قسم «الأكواد». **لا توجد أكواد جاهزة** (ما في `seed.sql` تطويري محلي فقط ومحظور على الإنتاج).

---

## ٦. دروس تشغيلية مدفوعة الثمن — لا تكررها

1. **النسخة المحلية تكون «قديمة» لا «مرتدّة» — ولم يضع شيء قط.** (مُصحَّح بتدقيق `git fsck`، 2026-08-24.)
   ظننتُ مراراً أن الحاوية «ارتدّت وأضاعت التزامات»، والحقيقة أدق وأقل إثارة: هذه الحاوية آخر التزام صنعته محلياً هو `401d8b8`، والالتزامات الأربعون التالية صُنعت في **حاويات أخرى ودُفعت مباشرة**، فلم تصل هذه النسخة قط. أي أنها **نسخة قديمة، لا تاريخ ممسوح**.
   **الدليل:** `git fsck --no-progress --dangling --unreachable` ⇐ **بلا مخرجات إطلاقاً**، و`git rev-list --all --not origin/<branch> | wc -l` ⇐ **صفر**. لا التزام واحد خارج ما هو مدفوع.
   **العلاج بسيط وغير مدمّر:** `git fetch origin` ثم `git merge --ff-only origin/<branch>` — والتقديم السريع **إضافي محض**، يستحيل أن يُضيّع عملاً.
   **لكن ابقَ على العادة:** ابدأ كل جلسة بـ`git fetch origin` وقارن، والتزم وادفع بخطوات صغيرة متكررة.
2. **لا تصدّق تقارير الوكلاء — قِس بنفسك.** وكيل ادّعى ٤٧٩/٤٧٩ والقياس أظهر ١٢٠/٤٧٩.
3. **الوكلاء يتشاركون مجلد git واحداً** ⇒ تتلوث رسائل الالتزام. دقّق بـ`git log --diff-filter=D`.
4. **`.next/` قديم يُنتج أخطاء tsc وهمية** تشير إلى ملفات محذوفة. الحل: `rm -rf .next`.
5. **خوادم `next start` يتيمة تبقى على المنفذ 3000.** أوقفها قبل أي فحص.
6. **موقع الإنتاج محجوب عن الشبكة هنا** ⇒ لا يمكن التحقق من النشر الحيّ من هذه الجلسة.
7. **حين يشير الخطأ إلى طرفك، اقرأ سجلّ الطرف الآخر قبل تعديل سطر واحد.** رسالة `No API key found` تصف عرضاً لا سبباً؛ السبب كان مسافة في إعداد لوحة Supabase، وسجلّ GoTrue كان يعرضه صراحةً منذ البداية. عُدِّل الكود مرتين قبل أن يُقرأ السجلّ — وكلا التعديلين كان بلا أثر.
8. **لا تكتب تشخيصاً لا يستطيع أن يفشل.** أول نسخة من `/api/health` قارنت `process.env.NEXT_PUBLIC_*` بنفسها وسمّت الطرفين «خادم» و«متصفح»؛ وTurbopack يطوي هذه القراءة وقت **البناء على الخادم أيضاً**، فاستحال اختلافهما وأعطى طمأنينة كاذبة. العلاج: قراءة بمفتاح **مُركَّب وقت التشغيل** (`env[name]`) لا بوصول منقوط. الآن يظهر `staleBundle` صراحةً.

---

## ٧. أوامر التحقق

```bash
git fetch origin && git status          # أولاً دائماً
rm -rf .next && npm install
npx tsc --noEmit && npm run lint
npm run build
npm run check                            # كل الفحوص الآلية
node scripts/check-paywall.js            # بعد البناء — تسريب المحتوى المدفوع
```

---

## ٨. الخطوة التالية المتفق عليها

المالك سيملي قائمة **الأخطاء الكارثية** ليبدأ إصلاحها. الأولوية المبدئية من التحليل:
1. 🔴 **اختيار المستوى** — المتطلب المخالَف؛ يغيّر معمار المسار والصلاحيات معاً
2. 🔴 **قاعدة البيانات** — بدونها لا دخول ولا لوحة ولا أكواد
3. 🔴 **ترتيب الكلمات تعليمياً** داخل الوحدات
4. 🔴 **الإنجليزية الحقيقية** للمحتوى لا للواجهة
5. 🟡 **تخطيط اللابتوب** (بعد تأكيد جودة الهاتف — فهو الأولوية)
